# Project Positive Vectors

This directory contains project-specific CDDL examples that are expected to pass parsing and linting.

Use these to cover repo-specific behavior that is not already represented by the RFC baseline corpus.

The `support/` subdirectory contains helper modules referenced by the import/include regression vectors in this tree.
