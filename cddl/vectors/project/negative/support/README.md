# Negative Support Fixtures

This directory contains helper CDDL files referenced by the negative recursive/include failure vectors.

These files are not meant to be used directly as top-level vectors.
