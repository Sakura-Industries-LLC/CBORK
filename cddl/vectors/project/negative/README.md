# Project Negative Vectors

This directory contains project-specific CDDL examples that are expected to fail parsing or linting.

Use these to cover invalid syntax, unsupported combinations, and regression cases that are specific to this repository.

The `support/` subdirectory contains helper modules used by the recursive failure fixtures in this tree.
