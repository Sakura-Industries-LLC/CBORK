# RFC Test Vectors

This directory holds the baseline test corpus derived from RFC-published CDDL examples and other standard-reference material.

Each vector should carry provenance in its filename or comments where that helps map it back to the source RFC, section, figure,
or example.
