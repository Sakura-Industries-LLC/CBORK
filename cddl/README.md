# CDDL Data

This directory groups the canonical CDDL support data for the parser and tests.

## Layout

* `rfc-std/` - standard-library CDDL files used as known include targets and shared support data
* `vectors/` - test vectors derived from RFC examples and project-specific coverage
